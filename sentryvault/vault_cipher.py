class Vault_Cipher:

    def encrypt (self, plain_text, key):
        #if the key is correct(matches the key of the user);
            #for every char in plain_text:
                #shift its index by 5 from the original order of alphabets
                #make sure you divide by 26 so that it remains in the correct order
        
        #return cipher_text

    def dencrypt (cipher_text, key):
        #if the key is correct(matches the key of the user);
        #for every char in plain_text:
            #shift its index by 5 (backwards) from the original order of alphabets
            #make sure you divide by 26 so that it remains in the correct order
        
        #return plain_text